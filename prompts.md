# Campus Wallet — Google AI Studio Prompt Log

This file records the five prompts used in the mock ZeroCode build. Prompts 1–5 were sent to Gemini in Google AI Studio Playground. The files were generated by Gemini; no manual code edits were made.

## Prompt 1 of 5 — Initial build

MOCK ZEROCODE — PROMPT 1 OF MAXIMUM 5. We have 70 minutes. Build Campus Wallet, an offline, single-page student expense tracker. Mandatory constraints: generate exactly three complete files named index.html, style.css, script.js; vanilla HTML/CSS/JavaScript only; no React, frameworks, libraries, CDN, external assets, npm, backend or APIs. All code must be generated by you; I will only save outputs verbatim and test, never manually edit code. HTML must link style.css and defer-load script.js.

Deliver a working first version now, with three clearly labelled fenced code blocks (one per file, no omitted sections). If native downloadable file attachments are supported, provide those too.

Features: add expenses with description, positive INR amount with max two decimal places, date and category (Food, Transport, Study, Other); list and delete expenses; set monthly budget; current-month total, remaining budget and category totals; month and category filters; localStorage persistence with graceful error handling; accessible labels and status feedback, responsive layout, empty state. Store money as integer paise; render descriptions as text, never HTML. Start empty, no invented personal data. Use a polished navy/teal visual style with system fonts. Keep implementation compact enough to output all three files without truncation.

After the files, give a brief manual test checklist with expected results for adding Food 120.50 and Transport 40, total 160.50, setting budget 1000 giving 839.50 remaining, reloading, filtering and deleting. Do not claim tests were executed unless actually executed. This is a mock exercise, not a live contest submission.

## Prompt 2 of 5 — Fix tested issues

PROMPT 2 OF MAXIMUM 5 — Fix findings from actual testing. The three files were saved verbatim and run together locally. PASS: Food 120.50 + Transport 40 totals 160.50; budget 1000 leaves 839.50; reload preserves; Food filter only shows Food; deleting Food leaves 40 total and 960 remaining. FAIL: at 375px wide there is horizontal overflow and a bottom horizontal scrollbar; overview/form cards extend beyond screen. Fix responsive sizing at 320px and 375px including grid/flex minimum widths, date/month inputs, stat values, expense rows and long text. Code review also found parseToPaise does not enforce Number.isSafeInteger, and loadState accepts arbitrary JSON shapes that could crash render. Add sensible money bounds (maximum INR 10000000 per value and safe totals), schema validation of stored expenses/budgets, meaningful persistent warning for unavailable storage, and an explicit hint when category filtering hides a newly added item. Preserve all required features, offline behavior, textContent rendering, and exactly three files. Output complete replacement style.css and script.js only if index.html needs no changes, otherwise all three complete files. No patch snippets, no libraries, no manual edits, no claims of executed tests. Keep output compact.

## Prompt 3 of 5 — Editing, search, and CSV

MOCK ZEROCODE — PROMPT 3 OF 5. Continue this exact Campus Wallet project using the preceding files as source of truth. Produce COMPLETE replacement index.html, style.css, script.js in three clearly labeled fenced code blocks; no snippets or omissions. No manual coding will be done. Stay vanilla HTML/CSS/JS, offline, no CDN, framework, library, API, or extra file; keep existing localStorage keys and existing data usable. Preserve all tested behavior: add/delete; integer paise; budget, monthly/category totals; month/category filters; persistence; 320px/375px layout; safe textContent rendering; stored-data validation.

Make the app substantially more useful with: (1) edit an existing expense via a clearly accessible Edit action; prefill form, Save/Cancel editing, preserve ID, update totals and storage, respect selected month/category; (2) case-insensitive text search of description layered with month and category filters, with a useful empty state and reset control; (3) Export current visible month to CSV using a Blob and local download, including only that selected month, proper RFC-style CSV quote escaping, column headers, integer-paise-to-INR decimal formatting, safe filename; no import or cloud service. Keep export available even for empty month with header only. Add a short budget progress indicator and helpful over-budget state.

Quality requirements: keyboard-usable labels and buttons, readable on desktop and phone, no horizontal overflow, no injection via user input, no mutation before failed validation. Do not show fabricated expenses. Keep code compact enough for complete output. After code, give only a short expected-results checklist. Do not claim execution of tests.

## Prompt 4 of 5 — Visual polish and Undo

MOCK ZEROCODE — PROMPT 4 OF 5. Continue the same Campus Wallet files from prompt 3. Produce COMPLETE replacement files for every file you change, each in its own labeled fenced code block; explicitly state unchanged filenames. No snippets, no manual edits. Keep exactly index.html, style.css, script.js, vanilla/offline, no libraries, APIs, CDN, fonts or extra files. Keep the current localStorage keys and every working feature: add/edit/delete, month/category/search filters, CSV export, budget progress, safe data handling and 320px mobile fit. Actual tests passed: edit Bus Pass to 45.25 updates total to 165.75 and remaining to 834.25; search semester matches it; CSV downloads valid comma-quoted row; no 375px overflow.

Make the UI feel like a polished contest finalist while staying fast and readable. Current desktop looks plain: month picker/export stack awkwardly, overview and form cards have excess blank height, category totals are tiny chips. Use clear typography with system fonts, cohesive navy/teal palette, stronger hierarchy, balanced spacing, deliberate responsive composition at 320/375/768/desktop, attractive empty state, visible focus and hover states, reduced-motion support. Introduce a compact visual category spending breakdown (accessible labeled CSS bars or SVG generated in JS, no canvas/library) driven by current-month totals. Add a concise monthly insight line computed from actual data (top spending category or no expenses). Do not invent data or break accessibility.

Also add Undo for deleting an expense: show a visible Undo action for 8 seconds; clicking it restores the same ID and original position and persists; a second delete replaces the pending undo. Keep normal status warnings readable and never hide a persistent storage warning behind a transient success toast. Avoid code or user input in innerHTML. If a required feature conflicts with polish, preserve the feature. Keep code complete and not truncated. After code, briefly list what changed; do not claim you ran tests.

## Prompt 5 of 5 — Complete final JavaScript

MOCK ZEROCODE — PROMPT 5 OF 5, FINAL. Your prompt-4 index.html and style.css are complete, but your script.js output was TRUNCATED mid-line after `editBtn.addEventListener('click', () => startEditExpense(exp.id));`. The incomplete JS was NOT installed. Produce ONE COMPLETE REPLACEMENT script.js code block ONLY, from `'use strict';` through final `DOMContentLoaded` listener and closing fence. No HTML/CSS, no prose before code. Keep it compact (target under 16,000 characters) so it finishes. All code must be generated by you; no manual editing. Use the exact prompt-4 HTML element IDs already present in the conversation: persistent-storage-banner, toast-banner, toast-message, toast-undo-btn, monthly-insight/insight-text, category-segmented-bar with seg-food/transport/study/other, cat-pct-food/transport/study/other, plus all prior form, filters, totals, progress and list IDs.

Required behavior: preserve legacy localStorage keys (`campus_wallet_expenses_v1`, `campus_wallet_budgets_v1`) and validate loaded shapes; add/edit/delete with Undo for 8 seconds and same ID, month/category/search filters, month-specific budget and totals, progress/overbudget state, top-category insight, segmented category bars, CSV current-month export with quote escaping and two-decimal integer-paise amounts, storage-error warning separate from transient toast, 320px-compatible rendering. Validate date/calendar, category, description and positive amount <= INR 10,000,000 before mutation; 0 budget must work; reject nonfinite/unsafe monthly sum rather than displaying a false ₹0.00. Render descriptions via textContent; use no user-derived innerHTML. CSV descriptions beginning =,+,-,@ should be protected from spreadsheet formula execution while preserving valid CSV. No API/framework/library/CDN/extra files. If unsure about the exact prompt-4 HTML IDs, reread your own prior response. Ensure the code ends syntactically and runs with that HTML. Output only the complete script.js fenced block, then a single line END OF SCRIPT. Do not claim tests were run.
